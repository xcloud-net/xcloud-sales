import {
  every,
  filter,
  find,
  first,
  take,
  groupBy,
  isArray,
  isEmpty,
  isString,
  isObject,
  join,
  last,
  lowerCase,
  map,
  max,
  min,
  sortBy,
  split,
  sum,
  sumBy,
  throttle,
  toString,
  replace,
  trim,
  trimEnd,
  trimStart,
  upperCase,
  startsWith,
  endsWith,
  forEach,
  flatMap,
  debounce,
  without,
  isNil,
  isNull,
  isUndefined,
  isNaN,
  findLastIndex,
  slice,
  some,
} from 'lodash-es';

export default {
  isNone: (x: any) => isNil(x) || isNull(x) || isUndefined(x) || isNaN(x),
  some,
  without,
  debounce,
  flatMap,
  every,
  forEach,
  filter,
  find,
  first,
  take,
  groupBy,
  isArray,
  isEmpty,
  isString,
  isObject,
  join,
  last,
  lowerCase,
  map,
  max,
  min,
  sortBy,
  split,
  sum,
  sumBy,
  throttle,
  toString,
  replace,
  trim,
  trimEnd,
  trimStart,
  upperCase,
  startsWith,
  endsWith,
  findLastIndex,
  slice,
};
